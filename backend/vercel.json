{
    "version": 2,
    "functions": {
      "backend/**/*.js": {
        "runtime": "@vercel/node@18.0.0", // Specify the version of the runtime
        "maxDuration": 60
      }
    },
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/backend/index.js"
      }
    ]
  }
  

import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    background: {
      default: '#292C33',
    },
    primary: {
      main: '#181823', 
    },
    secondary: {
      main: '#537FE7', 
    },
    tertiary: {
      main: '#C0EEF2', 
    },
    quaternary: {
      main: '#E9F8F9', 
    },
  },
});

export {theme}